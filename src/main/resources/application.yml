spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/excel_validator
    username: postgres
    password: Kedar@10396

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true